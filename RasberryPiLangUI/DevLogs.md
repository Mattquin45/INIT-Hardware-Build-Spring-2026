# Dev logs

## Raspberry Pi Language learning device

### The struggles to push the Scanner folder to the ScannerVisual branch on GitHub.

#### The problem:

To https://github.com/Mattquin45/INIT-Hardware-Build-Spring-2026.git
 ! [rejected]        ScannerVisual -> ScannerVisual (non-fast-forward)
error: failed to push some refs to 'https://github.com/Mattquin45/INIT-Hardware-Build-Spring-2026.git'
hint: Updates were rejected because a pushed branch tip is behind its remote
hint: counterpart. If you want to integrate the remote changes, use 'git pull'
hint: before pushing again.
hint: See the 'Note about fast-forwards' in 'git push --help' for details.

#### The solution:
I simply forced  the push. At the time I recording this issue, I now "miraculously" can push files to the branch with no issue at all.


### The "Wall of Red" in welcome.tsx

#### The problem:

I saw errors starting from line 8 to line 34 in welcome.tsx after runing `git checkout ScannerVisual`.

#### The solution:

